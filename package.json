{
  "name": "bedrock-chat-with-document",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "deploy": "pnpm --filter backend run deploy",
    "lint": "DEBUG=eslint:eslint eslint --fix --flag unstable_config_lookup_from_file . --fix"
  },
  "devDependencies": {
    "prettier": "3.4.2",
    "typescript": "5.7.2"
  },
  "prettier": {
    "tabWidth": 2,
    "semi": true,
    "singleQuote": false
  }
}
